<div class="elm-image">
	<a href="{{default link 'article.html'}}"><img src="{{thumb}}"></a>
</div>

<div class="elm-content">
	<div class="category">{{filter.category}}</div>
	<h2>{{title}}</h2>
	<p>{{teaser}}</p>
	
	<a href="{{default link 'article.html'}}" class="more-link">
	{{#is type 'video'}}
		View video
	{{else}}
		Read more
	{{/is}}
		<span class="icon"></span>
	</a>
</div>